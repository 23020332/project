<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Recipe App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Recipe App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addRecipe">Add new recipe</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>
 
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Recipe</h2></div>
        <br>
        <div class="text-center">
            <% if (recipe) { %>
                <form action="/editRecipe/<%= recipe.id %>" method="POST" enctype="multipart/form-data">
                    <label for="name">Recipe Name:</label><br>
                    <input type="text" id="name" name="name" value = <%= recipe.name %> required><br><br>
    
                    <label for="description">Recipe Description:</label><br>
                    <input type="text" id="description" name="description" value = <%= recipe.description %> required><br><br>
    
                    <label for="ingredient">Recipe Ingredient:</label><br>
                    <input type="text" id="ingredient" name="ingredient" value = <%= recipe.ingredient %> required><br><br>
    
                    <label for="instruction">Recipe Instruction:</label><br>
                    <input type="text" id="instruction" name="instruction" value = <%= recipe.instruction %> required><br><br>
    
                    <label for="image">Image:</label><br>
                    <input type="text" name="currentImage" value="<%= recipe.image %>" disabled><br>
                    <img src = "/images/<%= recipe.image %>" width="20%"><br><br>
    
                    New image:<br><input type="file" id="image" name="image" accept="image/*" %>"><br><br>
    
                    <button type="submit">Update Recipe</button>
                </form>
            <% } else { %>
                <p>No recipe found.</p>
            <% } %>
            <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>