<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Recipe App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Recipe App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/recipe">Add new recipe</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>
   
    <div class="container">
        <br>
        <div class="text-center"><h2>Recipe Details</h2></div>
        <br>
        <% if (recipe) { %> <!-- Check if recipe data exists -->
            <div>
                <p><b>Recipe Name:</b><br> <%= recipe.name %></p>
                <p><b>Recipe Image:</b><br> <img src = "/images/<%= recipe.image %>" width="50%"></p>
                <p><b>Recipe Description:</b><br> <%= recipe.description %></p>
                <p><b>Recipe Ingredient:</b><br> <%= recipe.ingredient %></p>
                <p><b>Recipe Instruction:</b><br> <%= recipe.instruction %></p>
            </div>
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        <a href ="/">Back</a>
    </div>
   
</body>
</html>
 